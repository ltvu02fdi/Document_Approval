<odoo>
    <record id="receipt_categories_list_view" model="ir.ui.view">
        <field name="name">receipt.categories.list.view</field>
        <field name="model">receipt.categories</field>
        <field name="arch" type="xml">
            <list>
                <field name="code" optional="show"/>
                <field name="name" optional="show"/>
                <field name="partner_id" optional="show"/>
                <field name="company_id" optional="show" groups="base.group_multi_company"/>
            </list>
        </field>
    </record>
    <record id="receipt_categories_form_view" model="ir.ui.view">
        <field name="name">receipt.categories.form.view</field>
        <field name="model">receipt.categories</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="code" required="1"/>
                            <field name="name" required="1"/>
                            <field name="active" invisible="1"/>
                        </group>
                        <group>
                            <field name="partner_id" options="{'no_edit': True, 'no_create': True}"/>
                            <field name="company_id" readonly="1" force_save="1" groups="base.group_multi_company"
                                   options="{'no_edit': True, 'no_create': True}"/>
                        </group>
                    </group>
                </sheet>
                <div class="o_attachment_preview"/>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="receipt_categories_action" model="ir.actions.act_window">
        <field name="name">Receipt Categories</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">receipt.categories</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>